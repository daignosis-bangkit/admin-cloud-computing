const init_state = {
};

const reducer = (state = init_state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export const data = init_state;
export default reducer;